# 🚀 최적화된 필수 라이브러리만 포함

# 기본 과학 연산 라이브러리 (최소한만)
numpy==1.26.4
opencv-python==4.11.0.86
Pillow==10.4.0
psutil==6.1.0
scikit-learn==1.3.2

# PyTorch는 Dockerfile에서 CPU 전용으로 설치합니다
# (--index-url https://download.pytorch.org/whl/cpu 필요)

# YOLOv8 (홀드 감지)
ultralytics==8.2.103

# CLIP AI 모델 (색상 분석)
git+https://github.com/openai/CLIP.git
ftfy
regex

# FastAPI & Backend
fastapi==0.119.0
uvicorn[standard]==0.37.0
python-multipart==0.0.20

# Celery & Redis (비동기 작업 처리)
celery>=5.3.0
redis==5.0.8

# OpenAI (GPT-4 Vision)
openai>=1.0.0

# 기타 필수 유틸
tqdm==4.66.5
pyyaml==6.0.2