# ClimbMate 전체 시스템 플로우 아키텍처
## 사용자 → AI 분석 → 결과 → 피드백 → 학습 전체 과정

---

## 🎯 다이어그램 구조 (한 화면에 모든 플로우)

```
┌─────────────────────────────────────────────────────────────┐
│  [상단] 사용자 입력 + 외부 요소                               │
│  [중앙] AWS EC2 (큰 박스)                                    │
│    └─ Docker Compose (내부 박스)                             │
│       └─ 모든 서비스들 + AI 처리 플로우                       │
│  [우측] 외부 API (OpenAI)                                    │
│  [하단] 피드백 학습 루프                                      │
└─────────────────────────────────────────────────────────────┘
```

---

## 📦 전체 컴포넌트 배치 (피그마용)

### 🌐 상단 - 사용자 접근

```
┌──────────────┐
│   👤 사용자   │
│  (클라이머)   │
└──────┬───────┘
       │ [1] HTTPS 요청
       ↓
┌──────────────────┐
│ climbmate.store  │
│  (도메인/Route53)│
└──────┬───────────┘
       │ [2] DNS 라우팅
       ↓
```

---

### 🟧 AWS EC2 박스 (노란색/주황색 테두리)

**박스 크기**: 900px × 850px
**위치**: 중앙
**라벨**: 좌측 상단 "Amazon EC2" (AWS 아이콘 포함)

---

### 🟦 Docker Compose 박스 (파란색 테두리, EC2 내부)

**박스 크기**: 850px × 750px
**위치**: EC2 박스 내부 중앙
**라벨**: 좌측 상단 "🐳 Docker Compose"

#### Docker 내부 배치

**1열 - 게이트웨이 & 프론트엔드 (상단)**
```
┌─────────────┐  ┌─────────────┐  ┌─────────────┐
│   Nginx     │  │   React     │  │  Let's      │
│   (녹색N)   │  │   ⚛️        │  │  Encrypt    │
│   :80,:443  │  │   :3000     │  │  🔒         │
└─────────────┘  └─────────────┘  └─────────────┘
```

**2열 - 백엔드 & AI 처리 (중앙)**
```
┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐
│  FastAPI    │  │   YOLOv8    │  │   규칙+ML   │  │   PyTorch   │
│   🐍        │  │   🎯        │  │   🎨 ★      │  │   🔥        │
│   :8000     │  │  홀드 감지   │  │  색상 분류   │  │             │
└─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘
```

**3열 - 비동기 & 데이터 (하단)**
```
┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐
│   Redis     │  │   Celery    │  │   SQLite    │  │   OpenCV    │
│   📮        │  │   🔄        │  │   💾        │  │   📷        │
│   :6379     │  │  비동기 작업  │  │   DB        │  │  이미지처리  │
└─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘
```

---

### 🟣 외부 API (EC2 박스 밖 - 우측)

```
┌──────────────────┐
│   OpenAI API     │
│   🤖             │
│  GPT-4 Vision    │
│  (외부 서버)      │
└──────────────────┘
```

---

## 🔗 전체 플로우 화살표 (번호 순서)

### 메인 플로우 1: 사용자 요청 → AI 분석

```
👤 사용자 (웹 브라우저)
  │
  │ [1] 클라이밍 벽 사진 업로드 (HTTPS)
  ↓
🌐 climbmate.store
  │
  │ [2] DNS → AWS IP
  ↓
╔═══════════════════════════════════════════════════════════╗
║ 🟧 AWS EC2 Instance                                       ║
║                                                           ║
║  ┌─────────────────────────────────────────────────────┐ ║
║  │ 🟦 Docker Compose                                   │ ║
║  │                                                     │ ║
║  │  [3] Port 443 → Nginx (🔒 SSL)                     │ ║
║  │    │                                                │ ║
║  │    ├─[4a] Port 3000 → React                        │ ║
║  │    │   (정적 파일 서빙)                             │ ║
║  │    │                                                │ ║
║  │    └─[4b] Port 8000 → FastAPI                      │ ║
║  │        (이미지 분석 API)                            │ ║
║  │                                                     │ ║
║  │  ┌─────────────────────────────────────────────┐  │ ║
║  │  │ 🤖 AI 처리 파이프라인                        │  │ ║
║  │  │                                             │  │ ║
║  │  │  [5] 이미지 전처리                          │  │ ║
║  │  │      FastAPI → OpenCV                       │  │ ║
║  │  │         ↓                                   │  │ ║
║  │  │  [6] 홀드 감지                              │  │ ║
║  │  │      YOLOv8 (+ PyTorch)                     │  │ ║
║  │  │      출력: Segmentation Masks               │  │ ║
║  │  │         ↓                                   │  │ ║
║  │  │  [7] 색상 분류 ★                           │  │ ║
║  │  │      규칙기반 HSV + ML 모델                 │  │ ║
║  │  │      (scikit-learn)                         │  │ ║
║  │  │      출력: 10색 분류                        │  │ ║
║  │  │         ↓                                   │  │ ║
║  │  │  [8] 난이도 분석 (3단계 하이브리드)         │  │ ║
║  │  │      ┌─────────────────────┐               │  │ ║
║  │  │      │ 1순위: 자체 ML      │               │  │ ║
║  │  │      │ (scikit-learn)      │               │  │ ║
║  │  │      └──────┬──────────────┘               │  │ ║
║  │  │             ↓ (실패 시)                     │  │ ║
║  │  │      ┌─────────────────────┐  ═══════════════════════╗
║  │  │      │ 2순위: GPT-4 Vision │ ←[9] API 호출  🟣 OpenAI║
║  │  │      │ (OpenAI API) ──────────────────────→ GPT-4   ║
║  │  │      └──────┬──────────────┘  ←[10] 분석결과 Vision ║
║  │  │             ↓ (실패 시)         ═══════════════════════╝
║  │  │      ┌─────────────────────┐               │  │ ║
║  │  │      │ 3순위: 규칙기반     │               │  │ ║
║  │  │      │ (통계 분석)         │               │  │ ║
║  │  │      └─────────────────────┘               │  │ ║
║  │  │                                             │  │ ║
║  │  └─────────────────────────────────────────────┘  │ ║
║  │         ↓                                         │ ║
║  │  [11] 결과 저장                                   │ ║
║  │       FastAPI ↔ SQLite 💾                        │ ║
║  │                                                   │ ║
║  │  [12] 비동기 처리 (긴 작업)                       │ ║
║  │       FastAPI → Redis :6379 → Celery             │ ║
║  │                                                   │ ║
║  └─────────────────────────────────────────────────┘ ║
║                                                       ║
╚═══════════════════════════════════════════════════════════╝
  │
  │ [13] JSON 응답
  ↓
React ⚛️
  │
  │ [14] 결과 렌더링
  ↓
👤 사용자
   (난이도, 유형, 홀드 정보 확인)
```

---

### 메인 플로우 2: 피드백 학습 루프

```
👤 사용자
  │
  │ [15] 피드백 제출
  │      - 색상 수정
  │      - 난이도 수정
  ↓
React ⚛️
  │
  │ [16] POST /api/feedback
  ↓
╔═══════════════════════════════════════════════════════════╗
║ 🟧 AWS EC2                                                ║
║  ┌─────────────────────────────────────────────────────┐ ║
║  │ 🟦 Docker Compose                                   │ ║
║  │                                                     │ ║
║  │  FastAPI 🐍                                         │ ║
║  │    │                                                │ ║
║  │    │ [17] 피드백 저장                               │ ║
║  │    ↓                                                │ ║
║  │  SQLite 💾                                          │ ║
║  │    - user_feedback                                  │ ║
║  │    - hold_color_feedback                            │ ║
║  │                                                     │ ║
║  │  [18] 데이터 축적 (30개+/50개+)                    │ ║
║  │    │                                                │ ║
║  │    │ [19] 충분한 데이터 확보                        │ ║
║  │    ↓                                                │ ║
║  │  ml_trainer.py                                      │ ║
║  │  (scikit-learn)                                     │ ║
║  │    │                                                │ ║
║  │    │ [20] 모델 재학습                               │ ║
║  │    │      - color_model.pkl                         │ ║
║  │    │      - difficulty_model.pkl                    │ ║
║  │    ↓                                                │ ║
║  │  [21] 새 모델 저장                                  │ ║
║  │    │                                                │ ║
║  │    │ [22] 캐시 리셋                                 │ ║
║  │    ↓                                                │ ║
║  │  FastAPI 🐍 (다음 분석부터 새 모델 적용) ─────┐    │ ║
║  │    ↑                                          │    │ ║
║  │    └───────────────────────────────────────────┘    │ ║
║  │                                                     │ ║
║  └─────────────────────────────────────────────────────┘ ║
╚═══════════════════════════════════════════════════════════╝
```

---

## 🎨 화살표 스타일 가이드

### 화살표 종류별 색상 & 스타일

| 플로우 | 색상 | 두께 | 스타일 | 사용처 |
|--------|------|------|--------|---------|
| 사용자 요청 | `#212121` (검정) | 4px | 실선 → | 사용자 → 도메인 → EC2 |
| 내부 데이터 | `#2196F3` (파랑) | 3px | 실선 → | Nginx → FastAPI, FastAPI → Redis |
| AI 처리 | `#FF6F00` (주황) | 3px | 실선 → | YOLO → 색상 → 난이도 |
| 외부 API | `#9C27B0` (보라) | 3px | 점선 ⇢ | FastAPI ⇢ OpenAI |
| 데이터베이스 | `#4CAF50` (초록) | 3px | 양방향 ↔ | FastAPI ↔ SQLite |
| 피드백 루프 | `#8BC34A` (연한 초록) | 3px | 곡선 ↻ | 피드백 → 학습 → 업데이트 |

---

## 📍 배치 좌표 가이드

### 캔버스 크기
```
전체: 1920px × 1400px (세로 길게)
```

### Y축 기준 배치

```
Y: 0-200px
  └─ 사용자 + 도메인

Y: 250-1150px
  └─ AWS EC2 박스 (900px × 850px)
      내부 (50px 패딩)
      └─ Docker Compose (850px × 750px)

Y: 250-1150px (우측)
  └─ OpenAI API 박스 (EC2와 같은 높이)

Y: 1200-1400px
  └─ 피드백 루프 설명 (선택사항)
```

### X축 기준 배치

```
X: 0-200px
  └─ 사용자 (중앙 정렬)

X: 250-1150px
  └─ AWS EC2 박스 (중앙)
      Docker 내부:
      - Nginx, React, Let's Encrypt (상단 3개)
      - FastAPI, YOLO, 규칙+ML, PyTorch (중앙 4개)
      - Redis, Celery, SQLite, OpenCV (하단 4개)

X: 1200-1500px
  └─ OpenAI API 박스 (우측)
```

---

## 🎯 개별 요소 크기

```
사용자 아이콘: 80px × 80px
도메인 박스: 200px × 60px
AWS EC2 박스: 900px × 850px (노란색 테두리 3px)
Docker 박스: 850px × 750px (파란색 테두리 3px)
OpenAI 박스: 250px × 150px (보라색 테두리 3px)

로고 박스: 90px × 90px
간격: 20px
포트 번호: 12pt (로고 하단)
```

---

## 📋 로고 및 아이콘 목록

### 외부 요소
| 위치 | 로고 | 이름 | 색상 |
|------|------|------|------|
| 상단 | 👤 | 사용자 | #333 |
| 상단 | 🌐 | climbmate.store | #2196F3 |

### AWS 박스
| 라벨 | 아이콘 | 색상 |
|------|--------|------|
| Amazon EC2 | AWS EC2 | #FF9900 |

### Docker 박스 (11개 로고)

**1열 (상단)**
| 로고 | 이름 | 포트 | 다운로드 |
|------|------|------|----------|
| 🟢 N | Nginx | :80, :443 | nginx.org |
| ⚛️ | React 19.1.1 | :3000 | react.dev |
| 🔒 | Let's Encrypt | - | letsencrypt.org |

**2열 (중앙)**
| 로고 | 이름 | 포트 | 다운로드 |
|------|------|------|----------|
| 🐍 | FastAPI 0.119.0 | :8000 | fastapi.tiangolo.com |
| 🎯 | YOLOv8-seg | - | ultralytics.com |
| 🎨 ★ | 규칙+ML (scikit) | - | scikit-learn.org |
| 🔥 | PyTorch | - | pytorch.org |

**3열 (하단)**
| 로고 | 이름 | 포트 | 다운로드 |
|------|------|------|----------|
| 📮 | Redis 7.x | :6379 | redis.io |
| 🔄 | Celery 5.3.0 | - | celeryproject.org |
| 💾 | SQLite | - | sqlite.org |
| 📷 | OpenCV 4.11+ | - | opencv.org |

### 외부 API
| 로고 | 이름 | 다운로드 |
|------|------|----------|
| 🤖 | GPT-4 Vision (OpenAI) | openai.com |

---

## 💡 추가 시각 요소

### 1. AI 처리 박스 (FastAPI 내부)

```
┌───────────────────────────────────────┐
│ 🤖 AI 처리 파이프라인                 │
│                                       │
│  1️⃣ OpenCV → 이미지 전처리            │
│  2️⃣ YOLOv8 → 홀드 감지 (95%+)        │
│  3️⃣ 규칙+ML → 색상 분류 (92%) ★      │
│  4️⃣ 하이브리드 → 난이도 분석          │
│     ├ 1순위: ML 모델                  │
│     ├ 2순위: GPT-4 Vision             │
│     └ 3순위: 규칙기반                 │
└───────────────────────────────────────┘

박스 위치: FastAPI 우측 또는 하단
크기: 300px × 200px
배경: rgba(255, 111, 0, 0.1) (연한 주황)
테두리: 2px dashed #FF6F00
```

### 2. 변경 사항 뱃지

```
★ CLIP → 규칙+ML
━━━━━━━━━━━━━━
정확도: 85% → 92%
속도: 200ms → 50ms
크기: 150MB → <5MB

위치: 규칙+ML 로고 옆
크기: 150px × 80px
배경: #FFC107
테두리: 2px solid #F57C00
```

### 3. 포트 라벨

각 서비스 로고 하단에:
```
예: Nginx
    :80, :443

스타일:
- 폰트: Monaco, Consolas (모노스페이스)
- 크기: 11pt
- 색상: #1976D2 (파랑)
- 배경: #E3F2FD (연한 파랑)
- 패딩: 4px 8px
- 라운드: 4px
```

### 4. 화살표 번호 배지

```
┌───┐
│ 1 │
└───┘

크기: 28px × 28px
배경: 흰색
테두리: 2px solid (화살표 색상)
폰트: 14pt Bold
위치: 화살표 중간 또는 시작점
```

### 5. 외부 API 표시

OpenAI 박스에 클라우드 표시:
```
☁️ 외부 API
dotted border
```

---

## ✅ 제작 체크리스트

### 박스 및 배경
- [ ] AWS EC2 박스 (노란색, 900×850px)
- [ ] Docker Compose 박스 (파란색, 850×750px)
- [ ] AI 처리 파이프라인 박스 (주황 점선)
- [ ] OpenAI 외부 API 박스 (보라색)

### 로고 배치 (11개)
- [ ] Nginx, React, Let's Encrypt (1열)
- [ ] FastAPI, YOLO, 규칙+ML★, PyTorch (2열)
- [ ] Redis, Celery, SQLite, OpenCV (3열)
- [ ] 사용자, 도메인 (외부 상단)
- [ ] OpenAI (외부 우측)

### 화살표 (22개 연결)
- [ ] 사용자 → 도메인 → EC2 (검정, 굵게)
- [ ] Nginx → React, FastAPI (파랑)
- [ ] FastAPI → YOLO → 규칙+ML → 난이도 (주황)
- [ ] FastAPI ⇢ OpenAI (보라, 점선)
- [ ] FastAPI ↔ SQLite (초록, 양방향)
- [ ] FastAPI → Redis → Celery (파랑)
- [ ] 피드백 → 학습 → 업데이트 (연한 초록, 곡선)

### 라벨 및 텍스트
- [ ] 포트 번호 (: 80, :443, :3000, :8000, :6379)
- [ ] 화살표 라벨 (DNS, API 호출, 저장 등)
- [ ] 화살표 번호 ([1], [2], ... [22])
- [ ] 처리 단계 (홀드 감지, 색상 분류 등)

### 강조 요소
- [ ] ★ 변경됨 뱃지 (CLIP → 규칙+ML)
- [ ] AI 처리 순서 (1️⃣2️⃣3️⃣4️⃣)
- [ ] 외부 API 표시 (☁️)
- [ ] 하이브리드 1/2/3순위

---

## 🎨 색상 팔레트 최종

```css
/* 박스 테두리 */
--ec2-border: #FF9900      /* AWS 주황 */
--docker-border: #2496ED   /* Docker 파랑 */
--external-border: #9C27B0 /* 외부 API 보라 */

/* 화살표 */
--user-flow: #212121       /* 사용자 플로우 */
--data-flow: #2196F3       /* 데이터 플로우 */
--ai-flow: #FF6F00         /* AI 처리 */
--external-api: #9C27B0    /* 외부 API */
--database: #4CAF50        /* DB */
--feedback: #8BC34A        /* 피드백 루프 */

/* 강조 */
--highlight: #FFC107       /* 변경 사항 */
--success: #4CAF50         /* 성공 */
```

---

작성일: 2025-10-27
프로젝트: ClimbMate
용도: 전체 시스템 플로우 아키텍처 (학술제 포스터)
스타일: 사용자 입력 → 처리 → 결과 → 학습 전체 과정


