# ClimbMate 전체 아키텍처 플로우 다이어그램
## 중첩 박스 + 로고 + 전체 상호작용

---

## 📦 박스 구조 (3단계 중첩)

```
┌─────────────────────────────────────────────────┐
│ 🟧 AWS Lightsail/EC2 (주황색 테두리)            │
│                                                 │
│  ┌───────────────────────────────────────────┐ │
│  │ 🟦 Docker Compose (파란색 테두리)         │ │
│  │                                           │ │
│  │  [모든 컨테이너와 서비스들]                │ │
│  │                                           │ │
│  └───────────────────────────────────────────┘ │
│                                                 │
└─────────────────────────────────────────────────┘
```

---

## 🎯 전체 컴포넌트 배치 (로고만)

### 외부 (박스 밖)

```
┌─────────────────────┐
│   👤 사용자          │
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│ 🌐 climbmate.store  │
│   (도메인)           │
└──────────┬──────────┘
           ↓
```

---

### 🟧 AWS EC2 박스 내부 (주황색)

**박스 상단 좌측 코너**
```
┌─────────────────┐
│ AWS Lightsail   │ ← 라벨 (박스 밖 또는 상단)
└─────────────────┘
```

---

### 🟦 Docker Compose 박스 내부 (파란색)

**박스 상단 좌측 코너**
```
┌─────────────────┐
│ 🐳 Docker       │ ← 라벨
└─────────────────┘
```

**박스 내부 배치 (3줄 구성)**

#### 1열 (상단 - 프론트엔드 & 웹서버)
```
┌──────────┐  ┌──────────┐  ┌──────────┐
│ Nginx    │  │ React    │  │ Vite     │
│ (녹색 N) │  │ (⚛️)     │  │          │
└──────────┘  └──────────┘  └──────────┘
```

#### 2열 (중앙 - 백엔드 & AI 모델)
```
┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐
│ FastAPI  │  │ YOLOv8   │  │ 규칙+ML  │  │ GPT-4    │  │ PyTorch  │
│ (🐍)     │  │          │  │ (scikit) │  │ (OpenAI) │  │ (🔥)     │
└──────────┘  └──────────┘  └──────────┘  └──────────┘  └──────────┘
```

#### 3열 (하단 - 인프라 & 데이터)
```
┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐
│ Redis    │  │ Celery   │  │ SQLite   │  │ NumPy    │  │ OpenCV   │
│ (📮)     │  │ (🔄)     │  │ (💾)     │  │          │  │          │
└──────────┘  └──────────┘  └──────────┘  └──────────┘  └──────────┘
```

#### 추가 (SSL)
```
┌─────────────────┐
│ Let's Encrypt   │
│ (🔒)            │
└─────────────────┘
```

---

## 🔗 전체 플로우 화살표 (번호 순서)

### 1️⃣ 사용자 요청 플로우

```
👤 사용자
  │
  │ [1] HTTPS 요청
  ↓
🌐 climbmate.store
  │
  │ [2] DNS → IP
  ↓
🟧 AWS EC2 (Port 443)
  │
  │ [3] 네트워크
  ↓
🟦 Docker 내부
  │
  │ [4] Port 443 → 80
  ↓
Nginx (🔒 Let's Encrypt)
  │
  ├─[5a] Port 3000 → React (정적 파일)
  │   │
  │   └─[5b] 사용자에게 HTML/JS 전달
  │
  └─[5c] Port 8000 → FastAPI (API 요청)
      │
      └─[6] 이미지 분석 시작
```

---

### 2️⃣ AI 처리 플로우 (FastAPI 내부)

```
FastAPI 🐍
  │
  │ [7] 이미지 전처리 (OpenCV + NumPy)
  ↓
YOLOv8-seg (+ PyTorch)
  │
  │ [8] 홀드 감지 완료
  ↓
규칙기반 HSV + ML 모델 (scikit-learn)
  │
  │ [9] 색상 분류 완료
  ↓
[하이브리드 난이도 분석]
  │
  ├─[10a] 1순위: 자체 ML 모델 (scikit-learn)
  │   │
  │   └─ 성공 → [13]
  │
  ├─[10b] 2순위: GPT-4 Vision (OpenAI API)
  │   │
  │   │ [11] 외부 API 호출
  │   │   → OpenAI 서버
  │   │   ← 분석 결과
  │   │
  │   └─ 성공 → [13]
  │
  └─[10c] 3순위: 규칙기반 분석 (통계)
      │
      └─ → [13]
```

---

### 3️⃣ 비동기 처리 플로우

```
FastAPI 🐍
  │
  │ [12] 긴 작업인 경우
  ↓
Redis 📮 (Port 6379)
  │
  │ [작업 큐에 추가]
  ↓
Celery Worker 🔄
  │
  │ [백그라운드 처리]
  │ - AI 분석 실행
  │ - 진행률 업데이트
  │
  └─[완료] → FastAPI → Redis → React
```

---

### 4️⃣ 데이터 저장/조회 플로우

```
FastAPI 🐍
  │
  │ [13] 분석 결과 저장
  ↓
SQLite 💾
  │
  │ [저장]
  │ - problems (문제 정보)
  │ - user_feedback (사용자 피드백)
  │ - hold_color_feedback (색상 피드백)
  │
  ↓
  │ [14] 결과 조회
  │
FastAPI 🐍
  │
  └─[15] JSON 응답 → React
```

---

### 5️⃣ 피드백 학습 플로우

```
👤 사용자
  │
  │ [16] 피드백 제출 (색상/난이도 수정)
  ↓
React ⚛️
  │
  │ [17] API 요청
  ↓
FastAPI 🐍 (/api/feedback)
  │
  │ [18] 피드백 저장
  ↓
SQLite 💾
  │
  │ [19] 데이터 축적 (30개+ / 50개+)
  │
  └─[20] 충분한 데이터 확보
      │
      ↓
ml_trainer.py (scikit-learn)
  │
  │ [21] 모델 재학습
  │ - color_model.pkl
  │ - difficulty_model.pkl
  │
  └─[22] 새 모델 적용 (다음 분석부터)
```

---

## 🎨 피그마 배치 상세

### 캔버스 구성

```
전체 캔버스: 1920px × 1200px

┌─────────────────────────────────────────────────────┐
│                                                     │
│  [상단 외부]                                         │
│  👤 사용자  →  🌐 도메인                            │
│                                                     │
│  ┌────────────────────────────────────────────┐    │
│  │ 🟧 AWS EC2 (주황색 박스)                   │    │
│  │   700px × 800px                            │    │
│  │                                             │    │
│  │  ┌──────────────────────────────────────┐  │    │
│  │  │ 🟦 Docker (파란색 박스)              │  │    │
│  │  │   650px × 650px                      │  │    │
│  │  │                                       │  │    │
│  │  │  [1열] Nginx  React  Vite            │  │    │
│  │  │                                       │  │    │
│  │  │  [2열] FastAPI YOLOv8 규칙+ML        │  │    │
│  │  │        GPT-4  PyTorch                 │  │    │
│  │  │                                       │  │    │
│  │  │  [3열] Redis Celery SQLite           │  │    │
│  │  │        NumPy OpenCV                   │  │    │
│  │  │                                       │  │    │
│  │  │  [하단] Let's Encrypt                │  │    │
│  │  │                                       │  │    │
│  │  └──────────────────────────────────────┘  │    │
│  │                                             │    │
│  └────────────────────────────────────────────┘    │
│                                                     │
└─────────────────────────────────────────────────────┘
```

---

## 📍 개별 컴포넌트 크기

```
로고 박스: 90px × 90px (정사각형)
간격: 20px
텍스트: 로고 하단 12pt

AWS 박스 (주황색):
- 크기: 700px × 800px
- 테두리: 3px solid #FF6F00
- 배경: rgba(255, 111, 0, 0.05) (5% 투명도)

Docker 박스 (파란색):
- 크기: 650px × 650px
- 테두리: 3px solid #2496ED
- 배경: rgba(36, 150, 237, 0.05)
- 위치: AWS 박스 중앙 상단
```

---

## 🔗 화살표 스타일

### 화살표 종류

#### 1. 사용자 플로우 (굵은 검정)
```
두께: 4px
색상: #212121
스타일: 실선 →
사용처:
- 사용자 → 도메인 → AWS → Nginx
```

#### 2. 데이터 처리 플로우 (파랑)
```
두께: 3px
색상: #2196F3
스타일: 실선 →
사용처:
- Nginx → React
- Nginx → FastAPI
- FastAPI → Redis → Celery
```

#### 3. AI 처리 플로우 (주황)
```
두께: 3px
색상: #FF6F00
스타일: 실선 →
사용처:
- FastAPI → YOLO → 색상 분류 → 난이도 분석
```

#### 4. 데이터베이스 (양방향, 초록)
```
두께: 3px
색상: #4CAF50
스타일: ↔ 양방향
사용처:
- FastAPI ↔ SQLite
```

#### 5. 외부 API 호출 (점선, 보라)
```
두께: 3px
색상: #9C27B0
스타일: 점선 → (dashed)
사용처:
- FastAPI → OpenAI (GPT-4 Vision)
```

#### 6. 피드백 루프 (곡선, 초록)
```
두께: 3px
색상: #8BC34A
스타일: 곡선 화살표 ↻
사용처:
- 사용자 피드백 → 학습 → 모델 업데이트
```

---

## 📋 로고 리스트 및 위치

### 외부 (AWS 박스 밖)

| 순서 | 로고 | 이름 | 색상 | 크기 |
|------|------|------|------|------|
| 1 | 👤 | 사용자 | #333 | 100px |
| 2 | 🌐 | climbmate.store | #2196F3 | - |

### AWS EC2 박스 (주황색 테두리)

| 박스 | 테두리 색 | 크기 |
|------|----------|------|
| AWS Lightsail/EC2 | #FF6F00 | 700×800 |

**라벨**: "AWS Lightsail/EC2" (박스 상단 외부 또는 좌측 상단 코너)

### Docker 박스 (파란색 테두리)

| 박스 | 테두리 색 | 크기 |
|------|----------|------|
| Docker Compose | #2496ED | 650×650 |

**라벨**: "🐳 Docker Compose" (박스 상단 좌측)

---

### Docker 박스 내부 - 1열 (상단)

| 순서 | 로고 | 이름 | 다운로드 |
|------|------|------|----------|
| 1 | 🟢 N | Nginx | nginx.org |
| 2 | ⚛️ | React 19.1.1 | react.dev |
| 3 | ⚡ | Vite 7.1.7 | vitejs.dev |

### Docker 박스 내부 - 2열 (중앙)

| 순서 | 로고 | 이름 | 다운로드 |
|------|------|------|----------|
| 1 | 🐍 | FastAPI 0.119.0 | fastapi.tiangolo.com |
| 2 | 🎯 Y | YOLOv8-seg | ultralytics.com |
| 3 | 🎨 | 규칙+ML (scikit-learn) | scikit-learn.org |
| 4 | 🤖 | GPT-4 Vision | openai.com |
| 5 | 🔥 | PyTorch | pytorch.org |

### Docker 박스 내부 - 3열 (하단)

| 순서 | 로고 | 이름 | 다운로드 |
|------|------|------|----------|
| 1 | 📮 | Redis 7.x | redis.io |
| 2 | 🔄 | Celery 5.3.0 | celeryproject.org |
| 3 | 💾 | SQLite | sqlite.org |
| 4 | 🔢 | NumPy | numpy.org |
| 5 | 📷 | OpenCV | opencv.org |

### SSL (박스 우측 또는 Nginx 옆)

| 로고 | 이름 | 다운로드 |
|------|------|----------|
| 🔒 | Let's Encrypt | letsencrypt.org |

---

## 🎯 화살표 연결 맵

### 주요 플로우 (번호 순)

```
[1] 사용자 → 도메인
    색상: #212121 (검정)
    두께: 4px

[2] 도메인 → AWS EC2 박스
    색상: #212121
    두께: 4px

[3] AWS 박스 → Docker 박스 (테두리 관통)
    색상: #2496ED (파랑)
    두께: 3px

[4] Docker 박스 → Nginx
    색상: #2196F3
    두께: 3px

[5a] Nginx → React
    색상: #2196F3
    두께: 3px
    라벨: "Port 3000"

[5b] React → 사용자 (역방향)
    색상: #2196F3
    두께: 3px
    스타일: 점선 (응답)

[5c] Nginx → FastAPI
    색상: #2196F3
    두께: 3px
    라벨: "Port 8000"

[6] FastAPI → OpenCV
    색상: #FF6F00 (주황)
    두께: 2px

[7] FastAPI → NumPy
    색상: #FF6F00
    두께: 2px

[8] FastAPI → YOLOv8
    색상: #FF6F00
    두께: 3px
    라벨: "홀드 감지"

[9] YOLOv8 → 규칙+ML
    색상: #FF6F00
    두께: 3px
    라벨: "색상 분류"

[10a] 규칙+ML → 자체 ML (1순위)
    색상: #FF6F00
    두께: 3px

[10b] 규칙+ML → GPT-4 (2순위)
    색상: #9C27B0 (보라)
    두께: 3px
    스타일: 점선 (외부 API)

[10c] 규칙+ML → 규칙기반 (3순위)
    색상: #FF6F00
    두께: 2px

[11] GPT-4 → 외부 OpenAI 서버
    색상: #9C27B0
    두께: 3px
    스타일: 점선
    라벨: "API 호출"

[12] FastAPI → Redis
    색상: #2196F3
    두께: 3px
    라벨: "Port 6379"

[13] Redis → Celery
    색상: #2196F3
    두께: 3px

[14] FastAPI ↔ SQLite (양방향)
    색상: #4CAF50 (초록)
    두께: 3px
    스타일: 양방향 ↔

[15] 사용자 → FastAPI (피드백)
    색상: #8BC34A (연한 초록)
    두께: 3px
    스타일: 곡선
    라벨: "피드백"

[16] FastAPI → SQLite (피드백 저장)
    색상: #8BC34A
    두께: 3px

[17] SQLite → ml_trainer (학습)
    색상: #8BC34A
    두께: 3px
    스타일: 곡선
    라벨: "모델 재학습"

[18] ml_trainer → FastAPI (새 모델 적용)
    색상: #8BC34A
    두께: 3px
    스타일: 곡선 ↻
```

---

## 🔢 화살표 라벨 표시

각 화살표 옆에 작은 원 또는 사각형 배지로 번호 표시:

```
배지 스타일:
- 크기: 24px × 24px
- 배경: 흰색
- 테두리: 2px solid (화살표 색상)
- 텍스트: 14pt Bold, 화살표 색상
- 위치: 화살표 중간
```

---

## 💡 추가 시각 요소

### 1. 변경 사항 뱃지 (CLIP → 규칙+ML)

"규칙+ML" 로고 옆에 노란색 별 표시:
```
★ 변경됨
배경: #FFC107
크기: 60px × 30px
위치: 로고 우측 상단
```

### 2. 처리 순서 표시

AI 파이프라인 화살표에 순서 번호:
```
1 → YOLO
2 → 색상 분류
3 → 난이도 분석
```

### 3. 외부 API 표시

GPT-4 로고에 작은 클라우드 아이콘 추가:
```
☁️ 외부 API
```

---

## ✅ 최종 체크리스트

### 박스
- [ ] AWS EC2 박스 (주황색, 700×800px)
- [ ] Docker 박스 (파란색, 650×650px)
- [ ] 중첩 배치 확인

### 로고 (총 17개)
- [ ] 사용자 (👤)
- [ ] 도메인 (🌐)
- [ ] Nginx (🟢)
- [ ] React (⚛️)
- [ ] Vite (⚡)
- [ ] FastAPI (🐍)
- [ ] YOLOv8 (🎯)
- [ ] 규칙+ML (🎨) ★
- [ ] GPT-4 (🤖)
- [ ] PyTorch (🔥)
- [ ] Redis (📮)
- [ ] Celery (🔄)
- [ ] SQLite (💾)
- [ ] NumPy (🔢)
- [ ] OpenCV (📷)
- [ ] Let's Encrypt (🔒)
- [ ] scikit-learn

### 화살표 (18개 주요 연결)
- [ ] 사용자 플로우 (6개)
- [ ] AI 처리 플로우 (5개)
- [ ] 비동기 처리 (2개)
- [ ] 데이터베이스 (1개 양방향)
- [ ] 외부 API (1개 점선)
- [ ] 피드백 루프 (3개 곡선)

### 라벨
- [ ] 포트 번호 (3000, 8000, 6379)
- [ ] 처리 단계 (홀드 감지, 색상 분류 등)
- [ ] 화살표 번호 또는 순서

### 강조
- [ ] "★ 변경됨" 뱃지
- [ ] 외부 API 표시 (GPT-4)
- [ ] 하이브리드 1/2/3순위 표시

---

작성일: 2025-10-27
용도: 피그마 전체 플로우 아키텍처 다이어그램
스타일: 중첩 박스 + 로고 배치 + 상호작용 화살표


